# Local test stack configuration
#
# Copy to .env and modify as needed:
#   cp .env.example .env
#
# These values are loaded by the Makefile and exported to docker-compose and tests.

# Relay port (host:container mapping uses this for host port)
NOSTR_PUBLISH_RELAY_PORT=8080

# Blossom server port
NOSTR_PUBLISH_BLOSSOM_PORT=3000
